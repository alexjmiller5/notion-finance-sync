__pycache__/
*.pyc
.venv/
.env
*.egg-info/
dist/
build/
.pytest_cache/
.ruff_cache/

# Per-bank Chrome user-data directories (cookies, profile, device-trust state)
data/sessions/

# Statement PDFs (sensitive financial data)
data/statements/

# Manual-entry CSVs for closed accounts
data/manual/

# Snapshotted HTML/JSON from each run for debugging
data/snapshots/

# Persistent health tracker + reward pool state
data/health.json
data/card_quarterly_pools.json

# Per-card reward rules (contains current choice categories, tier — personal)
config/cards.yaml

# SeleniumBase scratch
downloaded_files/

# macOS
.DS_Store

# Real transaction data used only as local test fixtures (sensitive)
tests/fixtures/bofa/*.html
tests/fixtures/bofa/deposit_activity_raw.json
# Fidelity real 401k capture (sensitive) — keep local only
tests/fixtures/fidelity/history_activity_real.json
tests/fixtures/us_bank/
tests/fixtures/etrade/activities_v2.json
tests/fixtures/etrade/espp_tables.json
tests/fixtures/bilt/transactions_v2.json
tests/fixtures/bilt/loyalty_activity.json

# Local non-secret config (personal Notion/1Password identifiers)
config.toml

# Stray snapshot path (login-failure screenshots)
src/data/
